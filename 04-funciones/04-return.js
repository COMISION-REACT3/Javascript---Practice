// el return permite que una funcion devuelva un valor con el cual puedo seguir trabajando luego

function obtenerArea(base, altura) {
    return base * altura
}

const area = obtenerArea(5,10);

console.log(area); // imprime 50